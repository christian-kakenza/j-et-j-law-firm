<%- include('header') %>

<main id="content" class="site-main">
    <section class="inner-banner-wrap">
        <div class="inner-baner-container" style="background-image: url(/images/principal1.jpg);">
            <div class="overlay"></div>
            <div class="container">
                <div class="inner-banner-content">
                    <!-- <h1 class="inner-title"><%= __("success_pages.confirmation") %></h1> -->
                </div>
            </div>
        </div>
    </section>

    <section class="success-page-section">
        <div class="container">
            <div class="success-container">
                <div class="success-icon">✓</div>
                <h1><%= __("success_pages.reservation.title") %></h1>
                <p><%= __("success_pages.reservation.message") %></p>
                
                <div class="contact-info">
                    <i class="fas fa-phone-alt"></i>
                    <%= __("success_pages.reservation.contact") %>
                </div>
                
                <div class="success-actions">
                    <a href="/" class="btn btn-primary">
                        <i class="fas fa-home"></i> <%= __("success_pages.back_to_home") %>
                    </a>
                    <a href="/contact" class="btn btn-secondary">
                        <i class="fas fa-envelope"></i> <%= __("success_pages.contact_us") %>
                    </a>
                </div>
                
                <div class="success-note">
                    <p><small><i class="fas fa-clock"></i> <%= __("success_pages.processing_note") %></small></p>
                </div>
            </div>
        </div>
    </section>
</main>

<%- include('footer') %>